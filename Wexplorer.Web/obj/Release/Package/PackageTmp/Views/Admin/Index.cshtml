@model IEnumerable<Wexplorer.Data.Entities.Post>
@{
    ViewBag.Title = "Index";
}
@Html.Partial("AdminMenu")
@foreach (var posts in Model)
{
    <div>
        <p>@Html.ActionLink(posts.Title, "EditPost", "Admin", new {id = posts.PostID }, null)</p>
    </div>
}

